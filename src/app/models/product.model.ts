export interface Product {
  productID: number;
  productName: string;
  price: number;
  stock: number;
  categoryID: number;
  productImagePath: string;
  averageRating: number;
reviewCount: number;
}